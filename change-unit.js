/* let unitBeingUsed = "metric";
const unit = document.querySelector("h2#temperature");
const degrees = document.querySelector("div#degrees");

// Main functions
unit.addEventListener("click", function () {
  $.getJSON(
    
  )
});
*/